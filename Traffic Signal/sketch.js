var yellow,red,green;
var rectangle;
function setup() {
  //size of the canvas to draw on
  createCanvas(innerWidth, innerHeight);

  // Set the background to white, and move the line to setup();
  background("white");

  fill("black");
  rect(100, 50, 100, 300); 
  rect(140, 350, 20, 300);

}


function draw() {
  
  red = new Signal(150, 100, "red");
  red.appear();
  
  /***Create objects for Green and Red lights*****/
  //Add code here: 
}
    

